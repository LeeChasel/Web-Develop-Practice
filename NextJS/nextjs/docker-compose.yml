version: '3'
services:
  nextjs:
    build: .
    container_name: "frontend"
    ports:
      - "64550:64550"
    volumes:
      - .:/home/nextJs
    working_dir : /home/nextJs
    command: bash -c "npm run dev"
    
